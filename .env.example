# Copy this file to .env for local settings.
# Do not place real API keys or private data in files committed to Git.

PRIVATE_AI_COMPANION_ENV=development

# Optional local VTube Studio plugin auth token.
# Keep the real value only in your local .env, never in Git.
PRIVATE_AI_COMPANION_VTS_TOKEN=
