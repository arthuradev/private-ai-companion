# Configs

Versioned configuration examples will live here.

Real local secrets belong in `.env`, which is ignored by Git. Keep committed
files generic and safe for a public repository.

Current versioned examples:

- `persona.default.toml`
- `providers.default.toml`
- `memory.default.toml`
- `speech.default.toml`
- `avatar.default.toml`
- `privacy.default.toml`
- `desktop.default.toml`
